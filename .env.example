# DSS Service Configuration

# Path to P12 certificate file (optional, defaults to cotelmur.p12)
P12_PATH=cotelmur.p12

# Password for P12 certificate (REQUIRED)
# IMPORTANT: Never commit the actual .env file with real credentials
P12_PASSWORD=your_password_here

# Timestamp Authority URL (optional, defaults to DigiCert)
TSA_URL=http://timestamp.digicert.com

# Server configuration (optional)
PORT=4000
